<?xml version="1.0"?>
<!DOCTYPE storm SYSTEM "..\storm.dtd">
<storm>
  <rules>

    <rule name="inventory_krat_deadVillager_1" Mode="and">
		<selectors>
			<hasName name="krat_deadVillager_1" />
		</selectors>
		<operations>
			<setInventory preset="inventory_krat_deadVillager_1" />
		</operations>
	</rule>

	<rule name="inventory_krat_krystofOderin" Mode="and">
		<selectors>
			<hasName name="krat_krystofOderin" />
		</selectors>

		<operations>
			<setInventory preset="inventory_krat_krystofOderin" />
		</operations>
	</rule>

	<rule name="inventory_krat_man_1" Mode="and">
		<selectors>
			<hasName name="krat_man_1" />
		</selectors>

		<operations>
			<setInventory preset="inventory_krat_man_1" />
		</operations>
	</rule>

	<rule name="inventory_krat_man_2" Mode="and">
		<selectors>
			<hasName name="krat_man_2" />
		</selectors>

		<operations>
			<setInventory preset="inventory_krat_man_2" />
		</operations>
	</rule>

	<rule name="inventory_closedCastle_ratbor">
        <selectors>
            <hasName Name="krat_closedCastle_ratbor_*" />
        </selectors>
        <operations>
            <setInventory preset="inventory_oderinSoldier_closedCastle" />
        </operations>
    </rule>

	


  </rules>
</storm>