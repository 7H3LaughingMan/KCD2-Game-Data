<?xml version="1.0"?>
<!DOCTYPE storm SYSTEM "..\storm.dtd">
<storm>
  <rules>

    <rule name="inventory_khor_oreseller" Mode="and">
        <selectors>
            <hasName name="khor_oreseller" />
        </selectors>
        <operations>
            <setInventory preset="inventory_khor_oreseller" />
        </operations>
    </rule>

    <rule name="inventory_khor_kristianZPisku" Mode="and">
			<selectors>
				<hasName name="khor_kristianZPisku" />
			</selectors>

			<operations>
				<setInventory preset="inventory_khor_kristianZPisku" />
			</operations>
		</rule>

		<rule name="inventory_khor_thomlin" Mode="and">
			<selectors>
				<hasName name="khor_thomlin" />
			</selectors>
			<operations>
				<setInventory preset="inventory_khor_thomlin" />
			</operations>
		</rule>

   
  </rules>
</storm>