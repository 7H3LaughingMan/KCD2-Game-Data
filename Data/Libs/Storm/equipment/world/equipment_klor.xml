<?xml version="1.0"?>
<!DOCTYPE storm SYSTEM "..\storm.dtd">
<storm>
  <rules>

    <rule name="inventory_poi_miriam" Mode="and">
      <selectors>
        <hasName name="klor_woman_2" />
      </selectors>
      <operations>
        <setInventory preset="inventory_poi_miriam" />
      </operations>
    </rule>

    <rule name="inventory_klor_jeronym" Mode="and">
			<selectors>
				<hasName name="klor_jeronym" />
			</selectors>
			<operations>
				<setInventory preset="inventory_klor_jeronym" />
			</operations>
		</rule>

  </rules>
</storm>