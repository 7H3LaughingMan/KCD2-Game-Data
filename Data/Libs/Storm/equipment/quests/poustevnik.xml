<?xml version="1.0"?>
<!DOCTYPE storm SYSTEM "..\storm.dtd">
<storm>
	<rules>

		<rule name="inventory_poustevnik_ambroz" Mode="and">

			<selectors>
				<hasName Name="tapo_ambroz" />
			</selectors>

			<operations>
				<setInventory preset="inventory_poustevnik" />
			</operations>

		</rule>

		<rule name="inventory_poustevnik_konrad" Mode="and">

			<selectors>
				<hasName Name="tapo_konrad" />
			</selectors>

			<operations>
				<setInventory preset="inventory_poustevnik_konrad" />
			</operations>

		</rule>
        
        
        		<rule name="inventory_poustevnik_niclas" Mode="and">

			<selectors>
				<hasName Name="poustevnik_niclas" />
			</selectors>

			<operations>
				<setInventory preset="inventory_niclas" />
			</operations>

		</rule>
        
        		<rule name="inventory_poustevnik_sebald" Mode="and">

			<selectors>
				<hasName Name="poustevnik_sebald" />
			</selectors>

			<operations>
				<setInventory preset="inventory_sebald" />
			</operations>

		</rule>
        
        		<rule name="inventory_poustevnik_arn" Mode="and">

			<selectors>
				<hasName Name="poustevnik_arn" />
			</selectors>

			<operations>
				<setInventory preset="inventory_arn" />
			</operations>

		</rule>
        
        		<rule name="inventory_poustevnik_clesgin" Mode="and">

			<selectors>
				<hasName Name="poustevnik_clesgin" />
			</selectors>

			<operations>
				<setInventory preset="inventory_clesgin" />
			</operations>

		</rule>


	</rules>
</storm>
