<?xml version="1.0"?>
<!DOCTYPE storm SYSTEM "..\storm.dtd">
<storm>
	<rules>

		<rule name="inventory_pytlakPtacek_corpseDeadBody" Mode="and">

			<selectors>
				<hasName Name="pytlakPtacek_poacherSlatejov" />
			</selectors>

			<operations>
				<setInventory preset="inventory_pytlakPtacek_corpseDeadBody" />
			</operations>

		</rule>

		<rule name="inventory_pytlakPtacek_CaponPreset" Mode="and">

			<selectors>
				<hasName Name="tkop_ptacek" />
			</selectors>

			<operations>
				<setInventory preset="inventory_tkop_ptacek" />
			</operations>

		</rule>

	</rules>
</storm>
