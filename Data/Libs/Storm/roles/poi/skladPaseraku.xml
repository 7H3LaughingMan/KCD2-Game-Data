<?xml version="1.0"?>
<!DOCTYPE storm SYSTEM "..\storm.dtd">
<storm>
	<rules>

		<rule name="skladPaseraku_smuggler">
			<selectors>
				<hasName name="skladPaseraku_smuggler" />
			</selectors>
			<operations>
				<addRole name="PASERAK_STARA_KUTNA" />
			</operations>
		</rule>
		
    </rules>
</storm>